// packages/passport-core/__tests__/smoke.test.ts
import { describe, it, expect } from "vitest";

describe("passport-core harness", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
